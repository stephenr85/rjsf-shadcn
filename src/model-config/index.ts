/**
 * Model Configuration Widgets Package
 * Specialized form widgets for AI/ML model parameter configuration
 */

// For now, just export placeholder functions
export const NumberSliderWidget = () => null;

// Widget registry for easy import
export const modelConfigWidgets = {
  NumberSliderWidget: NumberSliderWidget,
};

// Default export
export default modelConfigWidgets;